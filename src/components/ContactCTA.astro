---
// ContactCTA.astro - CTA alternatif LinkedIn
import { author } from '../sample'
import svgPinkStar from '../assets/pink-star.svg'
---

<section class="contact-cta py-16 md:py-24 px-4">
    <div class="max-w-2xl mx-auto">
        <!-- Card -->
        <div class="
            relative
            bg-white
            border-3 border-black
            p-8 md:p-12
            shadow-[8px_8px_0px_rgba(0,0,0,0.3)]
            text-center
        ">
            <!-- Corner decoration -->
            <img
                src={svgPinkStar.src}
                alt=""
                width="32"
                class="absolute -top-4 -right-4 opacity-80"
                aria-hidden="true"
            />

            <!-- Corner boxes -->
            <div class="absolute -top-1.5 -left-1.5 w-3 h-3 bg-[var(--violet)] border-2 border-black"></div>
            <div class="absolute -top-1.5 -right-1.5 w-3 h-3 bg-[var(--violet)] border-2 border-black"></div>
            <div class="absolute -bottom-1.5 -left-1.5 w-3 h-3 bg-[var(--violet)] border-2 border-black"></div>
            <div class="absolute -bottom-1.5 -right-1.5 w-3 h-3 bg-[var(--violet)] border-2 border-black"></div>

            <!-- Content -->
            <div class="mb-6">
                <span class="
                    inline-block
                    px-3 py-1
                    bg-[var(--accent)]
                    text-sm font-medium
                    mb-4
                ">
                    Alternative
                </span>
                <h3 class="text-2xl md:text-3xl font-bold mb-3">
                    Pas encore prêt à me contacter ?
                </h3>
                <p class="text-gray-600 max-w-md mx-auto">
                    Suivez-moi sur LinkedIn pour rester informé de mes projets
                    et réflexions sur la création digitale.
                </p>
            </div>

            <!-- CTA Button -->
            <a
                href={author.linkedin}
                target="_blank"
                rel="noopener noreferrer"
                class="
                    inline-flex items-center gap-3
                    bg-[var(--violet)]
                    text-white
                    border-2 border-black
                    px-8 py-4
                    font-bold
                    shadow-[4px_4px_0px_rgba(0,0,0,1)]
                    transition-all duration-200
                    hover:shadow-[2px_2px_0px_rgba(0,0,0,1)]
                    hover:translate-x-[2px] hover:translate-y-[2px]
                    hover:opacity-90
                "
            >
                <i class="ri-linkedin-box-fill text-xl"></i>
                <span>Suivre sur LinkedIn</span>
                <i class="ri-arrow-right-up-line"></i>
            </a>
        </div>
    </div>
</section>
